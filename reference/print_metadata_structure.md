# Render the structure category: one line per structure

Render the structure category: one line per structure

## Usage

``` r
print_metadata_structure(structure)
```

## Arguments

- structure:

  The structure list.
