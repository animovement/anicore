# The orientation role sets

`yaw` for a 2D frame; a unit quaternion (Hamilton, scalar first) for a
3D one (#46).

## Usage

``` r
list_orientation_role_sets()
```

## Value

Named list, kind to roles.
