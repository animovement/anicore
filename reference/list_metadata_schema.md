# The metadata layout each frame class must follow

The metadata layout each frame class must follow

## Usage

``` r
list_metadata_schema(
  class = c("anipoint", "anisegment", "anijoint", "anievent")
)
```

## Arguments

- class:

  Frame class name.

## Value

A list with `space` (logical) and `slots` (named list).
