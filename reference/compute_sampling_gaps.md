# The gaps between consecutive index values, within each key

Per key, because the index restarts in each group.

## Usage

``` r
compute_sampling_gaps(data)
```

## Arguments

- data:

  An anipoint object.

## Value

Numeric vector of gaps, empty when there are none to take.
