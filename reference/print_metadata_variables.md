# Render the variables category: one line per role, slots inline

Render the variables category: one line per role, slots inline

## Usage

``` r
print_metadata_variables(variables)
```

## Arguments

- variables:

  The variables list.
