# The axis role sets that define each coordinate system

The axis role sets that define each coordinate system

## Usage

``` r
list_axis_role_sets()
```

## Value

Named list mapping a comma-separated sorted role set to the coordinate
system it defines.
