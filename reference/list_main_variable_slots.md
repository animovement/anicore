# The slot a bare character vector declares, per role

The slot a bare character vector declares, per role

## Usage

``` r
list_main_variable_slots()
```

## Value

Named character vector, role to slot; `NA` when a role has no main slot.
