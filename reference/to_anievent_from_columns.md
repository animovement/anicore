# String-keyed kernel shared by `to_anievent` methods

String-keyed kernel shared by `to_anievent` methods

## Usage

``` r
to_anievent_from_columns(
  data,
  time_col,
  state_cols,
  point_cols,
  what_cols,
  when_cols,
  metadata
)
```
