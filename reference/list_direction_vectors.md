# Each direction as a unit vector

Right-handed basis; `right`, `up` and `back` (toward viewer) are
positive.

## Usage

``` r
list_direction_vectors()
```

## Value

Named list of length-3 numeric vectors.
