# Render one category's leaves as aligned name/type/value lines

Render one category's leaves as aligned name/type/value lines

## Usage

``` r
print_metadata_leaves(x)
```

## Arguments

- x:

  A named list of leaf values.
