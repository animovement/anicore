# Refuse roles and slots the frame's class does not carry

Refuse roles and slots the frame's class does not carry

## Usage

``` r
ensure_known_variable_slots(data, role, slots, call = rlang::caller_env())
```
