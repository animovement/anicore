# The spatial declaration of a legacy flat metadata list

`axes` wins over `variables_where` where it exists.

## Usage

``` r
legacy_where_position(md)
```

## Arguments

- md:

  A metadata list in the legacy flat layout.

## Value

Character vector, named by role when roles are known.
