# Which category each flat-addressable field lives in

`variables` slots are absent: `keys` is ambiguous between `what` and
`when`.

## Usage

``` r
list_metadata_field_categories()
```

## Value

Named character vector, field name to category.
