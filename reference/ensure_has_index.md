# Ensure the index column is present and numeric

Ensure the index column is present and numeric

## Usage

``` r
ensure_has_index(data)
```

## Arguments

- data:

  An anipoint object.

## Value

`TRUE`, invisibly.
