# Coerce links to a `from`/`to` table

Coerce links to a `from`/`to` table

## Usage

``` r
coerce_to_pair_df(x)
```

## Arguments

- x:

  A data frame with `from` and `to`, or a list of `c(from, to)` pairs.

## Value

A tibble with character `from` and `to`.
