# Drop spatial metadata an anievent cannot carry

Dropped rather than refused, since readers still pass them (#73).

## Usage

``` r
drop_spatial_metadata(metadata)
```

## Arguments

- metadata:

  Metadata supplied by the caller.

## Value

`metadata` without spatial fields.
