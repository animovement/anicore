# The default value of one flat-addressable metadata field

The default value of one flat-addressable metadata field

## Usage

``` r
default_metadata_leaf(field)
```

## Arguments

- field:

  Length-one character.

## Value

The field's default value, or `NULL` for an unknown field.
