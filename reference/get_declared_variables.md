# Columns declared by the metadata, keyed by role

Columns declared by the metadata, keyed by role

## Usage

``` r
get_declared_variables(md)
```

## Arguments

- md:

  An anipoint metadata list.

## Value

Named list of character vectors, one per declaration field.
