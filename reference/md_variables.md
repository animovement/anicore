# The variables category, in canonical list shape (legacy metadata translated)

The variables category, in canonical list shape (legacy metadata
translated)

## Usage

``` r
md_variables(md)
```

## Arguments

- md:

  A metadata list.

## Value

A named list of roles (`what`, `when`, `where`, `event`), each a named
list of slots.
