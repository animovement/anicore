% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structure.R
\name{structures}
\alias{structures}
\alias{set_structure}
\alias{get_structure}
\alias{remove_structure}
\title{Attach, read and remove the structures of a frame}
\usage{
set_structure(data, structure, variable = "keypoint", name = variable)

get_structure(data, name = NULL)

remove_structure(data, name)
}
\arguments{
\item{data}{An anipoint.}

\item{structure}{An \code{\link[=anistructure]{anistructure()}}.}

\item{variable}{The \code{what} or \code{when} key whose levels the points are.}

\item{name}{Name to store the structure under. Defaults to \code{variable}.}
}
\value{
\code{set_structure()} and \code{remove_structure()}: \code{data}, with the
structures updated. \code{get_structure()}: a named list of structures, or
one structure.
}
\description{
A frame can carry any number of named \code{\link[=anistructure]{anistructure()}}s, each spanning the
levels of one identity or temporal variable. Several may span the same
variable and overlap: a football frame can hold \code{team}, \code{defence} and
\code{left_flank} over \code{individual} alongside a \code{skeleton} over \code{keypoint}. A
structure's \code{points} are its members; levels it does not list are
outside it.
\itemize{
\item \code{set_structure()} attaches a structure under \code{name}, replacing one of
the same name.
\item \code{get_structure()} returns all structures as a named list, or one.
\item \code{remove_structure()} drops one.
}

Points not found in the data are kept with a warning, in case they are
recorded in another file.
}
\examples{
af <- example_anipoint(n_obs = 3, n_individuals = 2)
af <- set_structure(af, example_structure())
get_structure(af, "keypoint")

# Several structures over the same variable
af <- af |>
  set_structure(anistructure(points = c("1", "2")), "individual", "pair") |>
  set_structure(anistructure(points = "1"), "individual", "focal")
names(get_structure(af))

}
\seealso{
\code{\link[=anistructure]{anistructure()}}
}
