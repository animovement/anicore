% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unit_space.R
\name{convert_unit_space}
\alias{convert_unit_space}
\title{Convert the spatial unit of an anipoint}
\usage{
convert_unit_space(data, to_unit, calibration_factor = NULL)
}
\arguments{
\item{data}{An anipoint.}

\item{to_unit}{Target unit, one of the levels of \code{unit_space} in
\code{\link[=list_default_metadata]{list_default_metadata()}}.}

\item{calibration_factor}{Multiplier from the current unit to \code{to_unit}.
Derived between metric units; required when converting from \code{"px"}.}
}
\value{
\code{data}, rescaled, with \code{unit_space} updated.
}
\description{
Rescales the columns carrying a length and records the new \code{unit_space}.
Those are the length axes of the coordinate system — \code{x}, \code{y}, \code{z} on a
Cartesian frame, \code{rho} (and \code{z}) on a polar, cylindrical or spherical one.
Angular axes are \code{\link[=convert_unit_angle]{convert_unit_angle()}}'s. Declared \code{axis_extents} are
rescaled with them.

To declare a unit without changing values, use
\code{set_metadata(data, unit_space = "mm")}.
}
\examples{
af <- example_anipoint(n_obs = 3, n_individuals = 1, n_keypoints = 1)

# 1 px = 0.5 mm
af_mm <- convert_unit_space(af, "mm", calibration_factor = 0.5)
convert_unit_space(af_mm, "cm")

}
