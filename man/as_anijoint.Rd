% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anijoint.R
\name{as_anijoint}
\alias{as_anijoint}
\title{Convert segments to joints: one angle per joint}
\usage{
as_anijoint(data, structure = NULL)
}
\arguments{
\item{data}{An \code{\link[=as_anisegment]{as_anisegment()}} frame, or an anipoint, which is
converted to segments first.}

\item{structure}{For an anipoint, the structure to use; see
\code{\link[=as_anisegment]{as_anisegment()}}.}
}
\value{
An \code{anijoint}.
}
\description{
Computes the angle of each joint of a structure from the directions of
its two segments \code{a} and \code{b}, with \code{\link[=angle_between]{angle_between()}}:
\itemize{
\item In 2D, the signed angle turning from \code{a} to \code{b}, in \verb{[-pi, pi]},
positive from \code{x} toward \code{y}. \code{\link[=get_angle_direction]{get_angle_direction()}} says how that
looks on screen.
\item In 3D with no joint \code{axis}, the included angle, in \verb{[0, pi]}.
\item In 3D with an \code{axis} (\code{"x"}, \code{"y"}, \code{"z"} or a segment name), the
signed angle about it after projecting both segments onto the plane
perpendicular to it.
}

The angle is 0 when the two segments point the same way. The segment key
is replaced by a \code{joint} key, the angle is in the frame's \code{unit_angle},
and \code{confidence} is the lower of the two segments'.

A joint frame cannot be converted back: it keeps no lengths and, in 3D,
no rotation about the segments themselves.
}
\examples{
af <- example_anipoint(n_obs = 3, n_individuals = 1) |>
  set_structure(example_structure())
as_anijoint(af)

}
\seealso{
\code{\link[=angle_between]{angle_between()}}, \code{\link[=anistructure]{anistructure()}}
}
