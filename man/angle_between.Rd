% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/angle_between.R
\name{angle_between}
\alias{angle_between}
\title{The angle between two vectors}
\usage{
angle_between(u, v, axis = NULL)
}
\arguments{
\item{u, v}{Numeric vectors of length 2 or 3, or matrices or data frames
with 2 or 3 columns and one row per vector. A single vector is recycled.}

\item{axis}{\code{NULL}, or a vector (or one per row) of length 3.}
}
\value{
Numeric vector of angles in radians.
}
\description{
Row-wise angle between direction vectors, as used for joint angles.
\itemize{
\item With no \code{axis}, the included angle \verb{acos(u . v / |u||v|)}, in
\verb{[0, pi]}.
\item With an \code{axis}, both vectors are projected onto the plane perpendicular
to it, and the angle turns from \code{u} to \code{v} about the axis by the
right-hand rule, in \verb{[-pi, pi]}. For 2D vectors, \code{c(0, 0, 1)} gives the
signed angle from \code{x} toward \code{y}.
}

Vectors need not be unit length. A zero vector, or one parallel to the
axis, gives \code{NA}.
}
\examples{
angle_between(c(1, 0), c(0, 1))
angle_between(c(1, 0), c(0, 1), axis = c(0, 0, 1))
angle_between(c(1, 0), c(0, 1), axis = c(0, 0, -1))

# One angle per row
angle_between(rbind(c(1, 0, 0), c(0, 1, 0)), c(0, 0, 1))
}
