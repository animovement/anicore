% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/anisegment.R
\name{as_anisegment}
\alias{as_anisegment}
\title{Convert an anipoint to segments: a length and a direction per segment}
\usage{
as_anisegment(data, structure = NULL)
}
\arguments{
\item{data}{A Cartesian 2D or 3D anipoint.}

\item{structure}{Name of the structure to use. May be omitted when only
one structure has segments.}
}
\value{
An \code{anisegment}.
}
\description{
Re-expresses the positions of a structure's points as its segments. Each
row is one segment at one time: its \code{length} and the unit vector \code{ux},
\code{uy} (and \code{uz} in 3D) pointing from the segment's \code{from} point to its
\code{to} point, in the frame's axes and units.

The structure's variable (usually \code{keypoint}) is replaced by a \code{segment}
key. Structures over that variable other than \code{structure} are dropped;
those over the remaining keys are kept. A \code{confidence} column becomes the
lower of the two endpoints' values; other undeclared columns are
dropped.

\code{\link[=as_anipoint]{as_anipoint()}} rebuilds positions from the segments, anchored at the
root point's trajectory. That is useful after editing the segments — for
example holding each segment's length constant — since every point but
the root then moves to agree with them.
}
\examples{
af <- example_anipoint(n_obs = 3, n_individuals = 1) |>
  set_structure(example_structure())
seg <- as_anisegment(af)
seg

# Hold each segment's length constant, then rebuild the positions
rigid <- seg |>
  dplyr::mutate(length = stats::median(length))
rebuilt <- as_anipoint(rigid, root = af)

}
\seealso{
\code{\link[=anistructure]{anistructure()}}, \code{\link[=set_structure]{set_structure()}}
}
